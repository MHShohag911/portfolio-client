import React from 'react';

const AboutSectionTitle = ({heading}) => {
    return (
        <div>
            <h2 className='text-2xl font-bold text-center text-primary my-5'>{heading}</h2>
        </div>
    );
};

export default AboutSectionTitle;